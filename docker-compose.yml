services:
  job-tailor:
    build:
      context: ../..
      dockerfile: tools/job-tailor/Dockerfile
    env_file: .env
    privileged: true
    cap_add:
      - NET_ADMIN
      - SYS_MODULE
    devices:
      - /dev/net/tun
    volumes:
      - ./config:/app/tools/job-tailor/config
      - ./resume:/app/tools/job-tailor/resume
      - ./data:/app/tools/job-tailor/data
      - ./wg0.conf:/etc/wireguard/wg0.conf:ro
